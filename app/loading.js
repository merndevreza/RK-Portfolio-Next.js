 
const page = () => {
   return (
      <main className="flex justify-center items-center h-screen">
       <p className="text-[38px] md:text-[50px] xl:text-[65px] font-bold">Loading...</p>
    </main>
   );
};

export default page;